# NOTICE — malformed fixture

Authored for `eval/fixtures/broken/`. Not a real reference model and not a
transcription of one: it exists so the ALN rules annotated in `model.yaml` have
something to fire on.
